Copycopy code to clipboard
language: node_js
before_script:
  - npm install -g gatsby-cli
node_js:
  - "10"
deploy:
  provider: script
  script: yarn install && yarn run fix-medium-plugin && yarn run deploy
  skip_cleanup: true
  on:
    branch: master